{
  "name": "@z-wiki/desktop",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json && cp src/preload.cjs dist/preload.cjs",
    "start": "npm run build && electron ."
  },
  "dependencies": {
    "@z-wiki/server": "*"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "electron": "^38.0.0",
    "typescript": "~5.7.0"
  }
}
